# 项目企划书

## 1. 项目名称
代码仓库知识地图生成器（Repository Knowledge Map Generator）

## 2. 项目定位
本项目面向软件开发者、团队、新加入成员以及学生群体，目标是把一个陌生代码仓库转化为“可理解、可查询、可交付”的知识资产。

它不是简单的“AI 问答机器人”，而是一个能够帮助用户快速理解项目结构、模块职责、关键入口和开发约束的工程知识系统。

## 3. 解决的问题
在真实的软件开发场景中，很多人都会遇到以下问题：
- 进入一个新项目时，不知道从哪里开始阅读；
- 项目文档分散在 README、Wiki、PR、Issue 和团队聊天记录中，信息不统一；
- 新成员很难短时间内理解项目结构和开发约束；
- 项目维护者很难持续整理“项目为什么这样设计”的知识。

本项目希望通过 AI 自动分析仓库内容，生成一份“项目知识地图”，帮助用户快速建立对项目的整体理解。

## 4. 目标用户
- 新加入团队的开发者
- 需要快速理解项目的学生
- 项目维护者与技术负责人
- 开源项目贡献者
- 想把个人或团队项目知识沉淀下来的人

## 5. 价值主张
用户在 30 秒内可以得到以下信息：
- 这个项目解决什么问题；
- 这个项目的核心模块有哪些；
- 哪些文件最重要；
- 新人应该从哪里开始读；
- 这个项目有哪些关键开发约束。

## 6. 项目目标
### 6.1 核心目标
构建一个可运行的系统，支持以下流程：
1. 用户导入一个项目目录或代码仓库；
2. 系统分析项目结构与关键文件；
3. 自动生成一份项目知识摘要；
4. 用户可以对项目内容进行问答，快速获取信息。

### 6.2 具体功能目标
- 解析项目目录结构；
- 提取 README、配置文件、主要代码入口；
- 生成项目概览、模块说明、开发上手指南；
- 根据项目内容提供问答能力；
- 保存生成结果和历史记录，便于后续维护。

## 7. 核心功能模块

### 模块一：项目导入与解析
功能：
- 支持导入本地目录或指定的仓库路径；
- 读取文件树、README、配置文件、代码入口；
- 识别项目类型（前端、后端、全栈、工具类等）。

输出：
- 项目基本信息
- 目录结构摘要
- 关键文件列表

### 模块二：知识摘要生成
功能：
- 生成项目简介；
- 生成模块职责说明；
- 生成新人上手指南；
- 生成开发约束摘要。

输出：
- Markdown 文档
- 结构化摘要

### 模块三：项目问答
功能：
- 用户输入问题，例如：“这个项目如何启动？”
- 系统基于已导入的项目内容返回答案。

输出：
- 回答文本
- 相关文件引用

### 模块四：历史记录与知识管理
功能：
- 保存每次导入的项目内容；
- 保存每次生成的文档；
- 保存对话历史，便于复查和后续扩展。

## 8. 用户故事
1. 作为新加入的开发者，我希望快速理解项目结构，因此我能尽快开始开发。
2. 作为团队负责人，我希望把项目的关键背景和约束保存成可复用文档，因此新成员能更快上手。
3. 作为开源贡献者，我希望在不阅读整个仓库的情况下，快速了解该项目的主模块与开发方式。
4. 作为学生开发者，我希望用这个系统展示 AI 在工程知识治理中的实际应用。
5. 作为维护者，我希望能够把项目的设计意图和模块关系记录下来，从而减少知识流失。

## 9. 非功能性需求
- 性能：常见问答请求应在 5–10 秒内返回。
- 安全：LLM API Key 必须通过安全方式存储，绝不硬编码。
- 可用性：系统应支持本地运行和 Docker 运行。
- 可扩展性：后续可扩展支持 GitHub 仓库导入、更多生成模板、向量检索。
- 可维护性：代码结构清晰，模块边界明确，便于单人维护。

## 10. 技术选型
- 前端：React + Vite + Tailwind
- 后端：Python + FastAPI
- 数据库：PostgreSQL（MVP 可先用 SQLite，后续升级）
- 容器化：Docker Compose
- LLM：OpenAI / Azure OpenAI
- 测试：Pytest + 前端基础测试

## 11. 安全与凭据设计
- API Key 不直接写入源码，不提交到 Git。
- 优先通过环境变量或系统凭据存储。
- 所有敏感操作由后端统一处理。
- 日志中不暴露明文密钥。

## 12. 分发与部署
- 推荐使用 Docker 容器部署。
- README 中提供安装、运行、环境变量配置与测试命令。
- 支持本地开发与演示部署。

## 13. 项目里程碑
- 第一阶段：完成项目骨架与基础 API。
- 第二阶段：实现项目导入与解析能力。
- 第三阶段：实现知识摘要生成与项目问答。
- 第四阶段：补齐测试、Docker 与文档。

## 14. 风险与挑战
- LLM 可能产生幻觉，需要通过上下文约束降低错误率。
- 大型仓库分析可能较慢，需要限制扫描范围与使用缓存。
- 不同项目风格差异很大，需要模板化生成策略。

## 15. 预期成果
- 一个可运行的 Web 应用；
- 支持导入项目并生成知识摘要；
- 支持针对项目内容提问；
- 具备可演示、可测试、可交付的工程成果。





建立数据模型与持久化（SQLite 初版），实现 CRUD API：Project/Document/Conversation/Config。
将 knowledge generation 封装为后端模块：接受解析结果、调用 LLM、保存 Document。
实现 QA 模块：检索上下文（简单基于文本匹配或向量）、组装 prompt、返回答案带来源引用。
增加凭据管理 API（create/update/clear/status），并确保日志掩码敏感字段。
为上述功能写对应的 TDD 测试与 CI job（unit-test）。