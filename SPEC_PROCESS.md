# SPEC_PROCESS.md

## 1. brainstorming 关键节点

在本项目的早期设计阶段，最关键的澄清并不是“能做什么”，而是“项目要解决的真实问题是什么，以及哪些都是容易被 AI 误读的边界”。

关键问题包括：
- 用户的主诉求到底是“招人”还是“团队管理”？
- 队伍关系是否需要有成员审批、任务协作、披露与信誉机制？
- 平台是否需要实名制、学生认证、邮件验证？
- 是否要同时支持 AI 生成与知识库问答，还是只保留一个最核心的 AI 能力？

这些追问迫使我们收敛到：本项目不是单纯的招募平台，而是“校园项目协作闭环”：先有组队，再有任务协作，再有通知与审核，再有 AI 辅助知识整理。

## 2. 三轮关键迭代

### 迭代 1：从“报名平台”到“协作平台”
- AI 提议：先把功能控制在“队伍发布 + 匹配”两个模块，避免范围过大。
- 我的判断：这个范围太小，不能覆盖真实校园使用场景；如果不包含任务和通知，项目会变成 demo。
- 处理决策：扩展功能边界，加入任务协作、通知和管理审核，形成更完整的“校园组队与协作”闭环。

### 迭代 2：加入凭据安全要求后调整设计
- AI 认为可以直接用环境变量处理所有敏感配置。
- 我修正：仅将环境变量作为加载来源，并在 SPEC 中说明 `.env` 的明文风险，并要求在 README 和操作流程中明确安全边界。
- 处理决策：把“key 安全性”上升为核心工程要求，而非仅是开发方便。

### 迭代 3：从“单一功能应用”到“工程化分发”
- AI 最初偏向只保留本地开发说明。
- 我推翻：本项目需要支持 Docker/Compose 和 CI，不能只停留在源码交付。
- 处理决策：将分发、CI 与 README 一并纳入 spec与计划，确保从零运行和交付验收都成立。

## 3. AI 建议采纳与修正

采纳的建议：
- 把项目拆成独立模块：认证、队伍、任务、通知、AI。
- 在 AI 模块中加入 local fallback，避免 API Key 缺失直接崩溃。
- 为 MySQL 与前后端配置创建 `.env.example` 模板。

我修正或否定的建议：
- “把项目保持成一个超简版报名平台”，我认为不够真实且无法满足 B 类项目要求；因此扩展为协作平台。
- “只把 README 看作开发说明”——我要求它同时覆盖分发、key、安全边界、CI/CD。
- “A 模块与前端不必显式归档”——我要求保留 AI 知识地图的文档化输出，并写入 SPEC 与 PLAN。

## 4. 冷启动验证记录

本项目的冷启动验证采用第二个 agent，要求它仅依据 `SPEC.md` + `PLAN.md` 执行 1–2 个任务，不使用先前会话记忆。该 agent 对项目中的“角色边界”和“安全配置”提出了关键疑问：

- “队伍管理涉及什么权限边界？”
- “创建者、管理员、成员资格的状态区分是什么？”
- “AI Key 是否可为空？没有 key 时系统如何安全降级？”
- “Docker 是开发环境还是分发环境？”

它暴露的主要 spec 缺陷：
1. 之前规则说“平台支持组队”，但没有明确说明成员认证、审批、权限歧义；这会导致后续 subagent 随意实现。
2. `AI` 模块的 fallback 机制在 spec 中不够明确，导致 agent 可能忽略 unconfigured-key 场景。
3. 分发方式未形成明确落地约束：当前只说 Docker，但没有指出 `docker-compose` 是本地开发实例、Dockerfile 是交付产物。

我据此修订了 SPEC 与 PLAN：
- 补充了用户认证、管理员审核、任务协作的边界要求。
- 明确 AI 未配置 key 时必须保留降级逻辑，并在 README 说明。
- 把 Docker 分发与 Compose 运行分开写清，以满足“必须有分发产物 + README”要求。

## 5. 反思：brainstorming 的优点与不足

优点：
- 能快速逼出真实用户场景和边界问题。
- 对工程验收标准有帮助，避免直接去做“看似可用”的功能。
- 对凭据与安全设计的要求很有价值，因为它把本来会被遗漏的工程问题强制暴露出来。

不足：
- 如果没有强约束，AI 可能会把“功能范围”扩得过大，导致计划失控。
- 对“优先级”和“时间粒度”判断仍然依赖人类主动 steering。
- 在更复杂的 web + AI + 事件驱动场景里，单独的 brainstorming 仍需要后续 plan review 调整。

## 6. 总结

这个过程确认了：规范不是为了文档而写，而是为了在协作中减少假设、让新 agent 不在无定义处猜测。SPEC / PLAN 的真实价值在于把“模糊想法”转化为“可以验证的工程约束”。
