# AI 反思报告

这次项目的核心收获，并不是“AI 能顺利写出很多代码”，而是我第一次真正体会到：在 AI 协作开发中，真正决定项目成败的，不是生成代码的速度，而是能否把问题定义得足够清晰、边界划分得足够明确，并把验证机制嵌入流程。Superpowers 这套方法论最大的价值，恰恰在于它把“写代码”从工程主线中剥离出来，让我更早地面对需求、流程、审查和验收问题。最有帮助的技能是 `brainstorming` 和 `writing-plans`，因为它们迫使我把“用户是谁、解决什么问题、如何判断完成”说清楚，而不是直接把问题交给模型猜测。

从一开始，我就清楚地感受到：如果不做 spec 和 plan，AI 很容易在“看起来能跑”的前提下，快速产生大量不稳定实现。它能生成表面正确的代码，却很难知道哪些地方是真正的工程边界。`brainstorming` 解决的是目标模糊问题；`writing-plans` 解决的是执行失控问题。它们的作用不是帮我更快生成代码，而是让 subagent 在一个有边界、有验证、有回退的环境里工作。这一点对本项目非常关键，因为校园组队平台本身涉及用户认证、队伍管理、任务协作、通知、AI 辅助等多个功能模块，若没有清晰结构，很容易出现“功能看似都有了，但缺乏可审计性的实现”这种情况。

TDD 在这个项目里既是约束，也是一种质量放大器。最初我曾觉得 TDD 会拖慢节奏，尤其是测试还没写完时，AI 直接生成实现看起来更“高效”，但后来发现这恰恰是最危险的地方。AI 常常会在一组未被定义的假设上直接推断业务逻辑，最后做出的代码看起来正常，实则对边界条件、失败场景和回归测试几乎没有心理准备。TDD 让我们先明确输入、输出和失败场景，再由实现去满足它。这使开发过程更慢了一点，但它换来的是更稳定的工程状态，也让回归修复更容易控制。对单人 AI 协作项目来说，TDD 并不是“写很多测试”而已，而是一条约束 AI 盲目扩散的红线。

任务粒度是另一个关键经验。过大任务很容易让 subagent 在一次会话里同时处理架构、接口、前端和调试，最终得到的只是一个大而模糊的 patch；过小又会导致大量上下文切换、状态重建和重复解释。最优的粒度是：一个任务解决一个明确边界，时间控制在一轮会话内可完成，且具备清晰验证步骤。比如用户认证、队伍创建与加入、通知中心、AI 能力接入，都比较适合作为独立 task；而 CI、README、环境变量管理、Docker 分发则应该另起任务，不应混进功能开发里。分离边界之后，subagent 更容易保持焦点，也更容易复现失败并修复。

SPEC 和 PLAN 对实现质量的影响非常巨大。最典型的一个例子是 AI Key 缺失时的处理逻辑。在项目早期，如果我没有在 SPEC 中明确要求“AI key 缺失时必须进入 fallback，而不是直接抛错”，subagent 很可能会在无配置场景下直接报异常，进而导致前端大量无意义错误，接口行为也严重依赖临时假设。这个问题非常典型：规约不清，开发过程就容易偏离主线。后来我把安全边界、fallback 逻辑、配置读取步骤、环境变量风险写进 SPEC 和 PLAN 之后，后续的实现，至少在关键错误处理方面不再靠 AI 猜测。这说明工程质量不是靠代码多而是靠约束是否明确。

我还在这个项目里体会到一个更深的现实：凭据安全和分发要求并不是“文档工作”，它们是工程设计的一部分。`.env` 虽然方便，但它本身就是明文文件；终端历史、日志和容器环境都有泄露风险。项目需要说明“在全新机器上如何安全录入 key、如何复用配置、如何避免敏感信息进 Git 和日志”。这要求我在设计阶段就思考：哪些信息属于开发环境，哪些属于运行时环境；哪些配置应放在本地，哪些应该被展示给用户；如何在 Docker 和 CI 中保持最小权限。正因为这些要求，我把安全策略和运行链路写进 README 和 SPEC，而不再把它们当成最后收尾的杂项。

如果重做一次，我会更早锁定规范，并把 README 和 CI 纳入功能交付的一部分，而不是等实现大致完成后再补。更重要的是，我会让 AI 先面对失败测试，而不是“先写实现再找测试”。此外，我还会更强地要求 subagent 使用最小可验证改动，而不是一次性做大 patch。最后，我还认识到一个关键事实：Agentic SE 的真正价值，不在于让 AI 自动生成更复杂的代码，而在于让系统能够在模糊目标下仍然保持清晰边界、可验证结果和可审计过程。

对 Superpowers 这套方法论，我的判断是：它提供了一个很好的纪律框架，但它依赖人类负责定义问题。它不能替你回答“要做什么”，也不能替你判断“做对没有”。它假设人类能够形成初步的需求设计和工程边界，并通过审查机制来约束智能体。这个假设在本项目中基本成立，但前提必须是：你不能把“AI 能写代码”简单等同于“AI 能替你定义正确的工程目标”。真正的高级工程师角色，不是让模型代写全部代码，而是引导模型在正确的边界内做出正确判断，并对输出负责。

因此，这个项目最重要的收获不是“做出了一个可运行的应用”，而是我明白了：Agentic SE 的关键，不在于让 AI 更会写代码，而在于让工程系统在不确定性中仍保持清晰度、可验证性和可审计性。这个认识，将成为我后续与 AI 协作开发时最重要的底层原则。